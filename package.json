{
  "scripts": {
    "serve": "web-dev-server --node-resolve --watch --open",
    "dev": "pnpm run serve"
  },
  "dependencies": {
    "lit": "^3.1.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.3"
  }
}
